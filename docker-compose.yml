services:
  lb:
    image: haproxy:2.9-alpine
    volumes:
      - ./haproxy.cfg:/usr/local/etc/haproxy/haproxy.cfg:ro
      - sockets:/sockets
    ports:
      - "9999:9999"
    depends_on:
      api1:
        condition: service_started
      api2:
        condition: service_started
    deploy:
      resources:
        limits:
          cpus: "0.30"
          memory: "50MB"
    networks:
      - rinha

  api1:
    build:
      context: .
      args:
        ENABLE_PROFILER: ${ENABLE_PROFILER:-0}
    image: cleissonb/rinha-backend-2026-ispc:v0.1.0
    command: ["/bin/sh", "-lc", "umask 000 && exec /app/rinha-server"]
    hostname: api1
    environment:
      - PORT=8080
      - SOCKET_PATH=/sockets/api1.sock
      - HTTP_THREADS=2
      - INSTANCE=api1
      - PROFILE=${PROFILE:-0}
      - PROFILE_LOG_EVERY=${PROFILE_LOG_EVERY:-10000}
    volumes:
      - sockets:/sockets
    deploy:
      resources:
        limits:
          cpus: "0.35"
          memory: "150MB"
    networks:
      - rinha

  api2:
    build:
      context: .
      args:
        ENABLE_PROFILER: ${ENABLE_PROFILER:-0}
    image: cleissonb/rinha-backend-2026-ispc:v0.1.0
    command: ["/bin/sh", "-lc", "umask 000 && exec /app/rinha-server"]
    hostname: api2
    environment:
      - PORT=8080
      - SOCKET_PATH=/sockets/api2.sock
      - HTTP_THREADS=2
      - INSTANCE=api2
      - PROFILE=${PROFILE:-0}
      - PROFILE_LOG_EVERY=${PROFILE_LOG_EVERY:-10000}
    volumes:
      - sockets:/sockets
    deploy:
      resources:
        limits:
          cpus: "0.35"
          memory: "150MB"
    networks:
      - rinha

networks:
  rinha:
    driver: bridge

volumes:
  sockets:
